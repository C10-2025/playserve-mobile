import 'package:flutter/material.dart';

class AdminUsersPage extends StatelessWidget {
  const AdminUsersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin - Users')),
      body: const Center(child: Text('Admin Users Screen (Coming Soon)')),
    );
  }
}

class AdminReviewsPage extends StatelessWidget {
  const AdminReviewsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin - Reviews')),
      body: const Center(child: Text('Admin Reviews Screen (Coming Soon)')),
    );
  }
}

class AdminCommunityPage extends StatelessWidget {
  const AdminCommunityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin - Community')),
      body: const Center(child: Text('Admin Community Screen (Coming Soon)')),
    );
  }
}
